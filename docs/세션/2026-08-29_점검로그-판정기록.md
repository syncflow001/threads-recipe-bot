---
type: 세션
date: 2026-08-29
주제: 점검 로그 — 컨트롤러가 내린 판정 전부
---
# 판정 기록 — 점검 로그 (2026-08-29)

## 한 일

계획 실행 중 컨트롤러가 사용자를 대신해 내린 판정 **스물다섯 개를 빠짐없이** 옮겨 적었다.
원래 장부는 지웠고 사용자에게는 요약만 전했었다 — 그 잘못을 바로잡는 문서다.

## 결정 (왜 그렇게 했나까지)

계획 실행 중 컨트롤러가 사용자를 대신해 내린 판정을 **빠짐없이** 남긴다.
원래 장부(`.superpowers/sdd/…/progress.md`)는 지웠다 — **지우기 전에 여기 옮겼어야 했는데 요약만 전했다.**
그 잘못을 [[판정은-요약하지-말고-빠짐없이-남긴다]] 에 적었다.

## 첫 계정 이름 붙이기 (2026-08-28, 병합 `7606caf`)

| # | 판정 | 왜 | 틀리면 |
|---|---|---|---|
| 1 | 엔진 107곳을 안 뜯고 「빈 첫 계정은 목록에서 뺀다」 한 줄 + 파일 옮기기로 간다 | 규칙을 통째로 바꾸는 것보다 싸다 | `run.mjs` 를 인자 없이 돌리면 열쇠 없이 돈다(문서에 적음) |
| 2 | 쿠팡 꼬리표 `t`→`examplec` 바뀜을 받아들인다 | 쿠팡이 SubID 를 빈 값으로 준다(실측) | 지난 통계와 끊김 — 실제 영향 없음 |
| 3 | launchd 를 `시각표켜기`/`감시켜기` 로 안 켜고 옛 plist 세 곳만 바꾼다 | 그 함수들이 주입을 안 받아 **검사가 실서버를 건드린다** | 새 plist 에 키가 빠질 수 있다 — 백업으로 되돌린다 |
| 4 | 계획의 `logs/알림기록.main.jsonl` 자리를 뿌리로 바로잡는다 | `src/조회수알리미.mjs:26` 실측 | 없는 파일을 찾는다 |
| 5 | **계획 표가 틀렸다 — `미디어지문.json` 은 계정별이다. 옮긴다** | `src/미디어지문.mjs:35`·`run.mjs:54`·`src/계정.mjs:153` | **안 옮기면 이미 올린 사진을 다시 올린다** |
| 6 | `올린요리.json` 은 안 옮긴다 | 엔진이 안 읽는 죽은 파일 | 죽은 파일이 남는다 |
| 7 | `media/막힌것` 은 안 옮긴다 | `src/관문.mjs:5` 의 뿌리가 저장소 뿌리 | 공용 폴더가 한 계정에 묶인다 |
| 8 | `run.mjs:44~47` 낡은 주석을 고친다 | 그 주석이 계획을 오답으로 끌고 갔다 | 다음 사람이 또 속는다 |
| 9 | 함수 길이(105·80줄)는 그대로 둔다 | 순서가 이 도구의 핵심 | 읽기 어려움이 남는다 |
| 10 | Minor 둘(ENOTEMPTY·CRLF)은 미룬다 | 실제 `.env.local` 이 LF | 아주 드문 가장자리 |
| 11 | 「고침 판 한 번」 원칙을 어기고 한 번 더 받는다 | Task 3 이 되돌릴 수 없는 데이터를 옮긴다 | 6줄이 느는 것뿐 |
| 12 | 실제 옮기기는 검사 구멍을 막은 뒤에 한다 | 유령 라벨과 부딪혀 반쪽 상태가 된다 | — |

## 점검 로그 (2026-08-29, 병합 `ffd77ab`)

| # | 판정 | 왜 | 틀리면 |
|---|---|---|---|
| 1 | 과제 6 에도 launchctl diff 증거를 요구한다 | 계획서엔 4·5 만 적혔으나 6 도 launchd 를 만진다 | 증거 한 줄이 는다 |
| 2 | 계정 칸 없는 옛 막힌 글 3편은 계정별 화면에서 뺀다 | 안 빼면 모든 계정에 계속 뜬다 | 그 3편이 어느 화면에도 안 보인다(파일은 남음, 문서에 적음) |
| 3 | 계획서의 `진행알림` 은 실제 이름 `알림담기` 를 쓴다 | 이름을 바꾸면 부르는 곳 넷을 더 건드린다 | 계획서 문구와 코드가 어긋난 채 남는다 |
| 4 | `진행끝(값, 계정)` 으로 인자를 늘린다 | 계정별로 쪼개면 어느 계정을 끝낼지 알아야 한다 | 한 곳만 빠져도 첫 계정 칸을 조용히 끝낸다 |
| 5 | 검사가 실제 LaunchAgents·Keychain 을 **읽는** 것은 미룬다 | 읽기뿐이고 이 갈래가 만든 길이 아니다. diff 가 네 번 다 비었다 | 검사가 그 맥 상태에 흔들린다 |
| 6 | 과제 4 의 검사 공백을 채운다(계획은 넷만 요구) | **그 공백이 헛알람 둘을 통과시킨 것이 증명됐다.** 설계 §7 이 이긴다 | 검사가 몇 개 는다 |
| 7 | **`한판` 이 `전체점검` 을 반드시 부른다** | 설계 §9 성공기준 1. 안 부르면 **핵심 목표가 첫 판부터 안 이뤄진다** | 알림 한 통이 는다 |
| 8 | 파수꾼이 죽는 경우 둘을 고친다 | 점검기가 멈추면 알려 줄 사람이 없다 | try/catch 몇 줄이 는다 |
| 9 | **켜는 화면이 없는 것은 진짜 빠짐이다** | 설계 §4-5. `점검켜기` 부르는 곳이 0이라 **기능 전체에 닿을 수 없었다** | 카드 하나가 는다 |
| 10 | 거짓 초록불을 고친다 | **이 기능이 막으려는 실패가 정확히 그것이다** | 오류 분기 한 줄이 는다 |
| 11 | 최종 검토 12건을 한 판에 다 고친다 | 두 번째 고침 판이 없다. 전부 1~5줄이고 같은 줄기 | 새 결함이 들 수 있어 재검토를 붙였다 |
| 12 | 재판정 — 판정 2 의 전제를 정정 | 「전부 보기」 화면 경로가 없다(`blocked/route.ts:10`) | 문서에 적어 갈음 |
| 13 | 재판정 — 판정 5 미룸 유지 | 검토가 추적해 기존 경로임을 확인 | 별도 과제로 남김 |

## 미룬 Minor (최종 검토가 「그대로 둬도 된다」고 판정)

`관문.mjs` 의 `writeFile` · `상태통.계정별` 안 지움 · `update/run` 바인딩 · `실행고르기` 중복 ·
`열쇠::…` 꼴 · `기록더하기` 전체 재작성 · 발행 칸 2시간 창 · `무엇` 에 plist 라벨 원문 ·
`stop` 라우트 · `점검.sh` 가 `.env.local` 없으면 죽는 것.

## 다음 액션

- [ ] 없다. 이 문서는 기록용이다. 재개 지점은 `docs/세션/2026-08-29_점검로그.md` 「다음 액션」 을 본다

## 남은 한계

- **점검기 자신이 죽으면 맥 밖에서 알려 줄 장치가 없다.** 화면의 「마지막으로 돈 때」가 낡으면 빨갛게 뜨지만 화면을 열어야 보인다.
- 텔레그램이 끊긴 동안 매 판 같은 「생김」 줄이 이력에 쌓인다(자르기가 무한 팽창은 막는다).
- 개요 「상태 체크」는 화면 조각이라 자동 검사가 없다.
