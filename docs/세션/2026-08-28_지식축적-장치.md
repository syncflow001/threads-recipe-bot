---
type: 세션
date: 2026-08-28
주제: 지식 축적 장치
---
# 세션 2026-08-28 — 지식 축적 장치

## 한 일
- CLAUDE.md 의 「문서 다섯, 여섯째 금지」 규칙을 없앴다.
- `docs/교훈/` (카드 한 장 = 교훈 하나, `_형식.md`·`색인.md`) 을 만들었다.
- 자비스 풀스택의 장치 다섯을 읽고 우리 것으로 새로 썼다 — `docs/세션/` · `도구.세션부팅.sh` (SessionStart 훅) · `도구.문서점검.mjs` · 클로드 메모리에 위치 카드.

- 배울점 문서(08-24)의 「빠진 것」 아홉을 대조했다 — 일곱은 이미 코드로 들어가 있다 (멈춤·헬스체크·관문·도달·길들이기·진단·관문 시험).

## 알게 된 것 (검토 결과 — 고치지 않았다)
1. **장부에 원인 칸이 없다.** `src/성적.mjs` 는 결과(조회·좋아요·답글)만 찍고, 그 글이 나간 조건(원글 등급·링크 위치·말투 팩·발행 시각·사진 유무)은 안 찍는다. 이게 없으면 「무엇이 조회수를 올렸나」 카드를 영영 못 쓴다. 가장 먼저 손볼 곳
2. 글별 수익은 여전히 안 잡힌다 (HANDOFF §7-2, subId 빈 값). 쿠팡 파트너스 웹 화면 SubID 보고를 사용자가 직접 확인해야 한다
3. HANDOFF 에 「재개 지점」 제목이 셋 (231줄·2736줄·한눈에). 앞의 둘은 끝난 일이다 — 제목을 「끝남」 으로
4. ~~사용법이 옛 7788 을 가리킨다~~ — 틀린 판단이었다. 같은 날 다른 세션이 ⑥ 에서 새 대시보드를 7788 로 바꿔치기해 사용법이 맞다. **낡은 갈래에서 검토하고 main 과 안 맞춰 본 것이 원인** — 검토는 main 을 합친 뒤에 한다
5. `test.mjs` 2,940줄 (800줄 규칙 초과, CHECKLIST 73줄). `설정화면-html.mjs` 는 ⑥ 에서 archive 로 가 해당 없음
6. `도구.문서점검.mjs` 는 문서끼리만 본다. 문서↔코드 어긋남(4번 같은 것)은 못 잡는다 — 한 번 더 생기면 그때 붙인다

## 결정 (왜 그렇게 했나까지)
- 인계 `HANDOFF.md` 는 더 키우지 않는다. 2,823줄이라 통째로 읽을 수 없다. 앞으로 세션 기록은 `docs/세션/날짜_주제.md` 한 장씩. 인계는 「한눈에」 절만 손본다.
- 훅은 최근 세션의 「다음 액션」과 교훈 색인만 넣는다. 전부 넣으면 읽지 않는다.
- 자비스 코드는 가져오지 않았다 (유료·재배포 금지, CLAUDE.md §3). 설계만 옮겼다.

## 다음 액션
- [ ] 발행 장부에 원인 칸(원글 등급·링크 위치·말투 팩·발행 시각·사진 유무)을 적는다 — 어떤 칸을 적을지 사용자와 먼저 정한다
- [ ] HANDOFF 327줄·2832줄의 낡은 「재개 지점」 제목을 「끝남」 으로 (살아 있는 재개 지점은 57줄 첫 계정 이름 붙이기 하나)
- [ ] HANDOFF.md 47군데(뒤집음·실패·함정)에서 교훈 카드 초안을 뽑는다 — 사용자 승인 뒤
- [ ] 쿠팡 파트너스 웹 화면에서 SubID 별 보고서를 사용자가 직접 확인 (인계 §7-2). 글별 수익이 잡혀야 「무엇이 돈이 되나」 카드를 쓸 수 있다
- [ ] 카드가 20장 넘으면 `색인.md` 를 태그별로 묶는다
관련: HANDOFF §7-2 · docs/superpowers/specs/2026-08-24-자비스풀스택-배울점.md
