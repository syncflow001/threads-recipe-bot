'use client'
// 활동설명 문단 — 모든 계정에 같은 안내를 준다(옛 길들이기그리기 1572~1577). 엔진은 한국어도 이미 지원한다
import { 귀띔 } from '@/components/공용/카드'

export function 활동설명({ tame }: { tame?: { 언어: string; 간격시간: number } }) {
  if (!tame) return null
  return (
    <귀띔>
      버튼을 누르면 그 자리에서 한 번 돌아요. 뭘 하고 있는지는 <b>발행</b> 쪽 「돌려 본 기록」에 뜹니다.
      {' '}홈 피드가 이 계정의 <b>언어와 분야</b>로 채워지도록 사람처럼 들락거립니다. 한국어 계정도 씁니다 —
      피드가 요리 쪽이어야 쓸 글이 걷힙니다.
    </귀띔>
  )
}
